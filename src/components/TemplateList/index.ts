/**
 * TemplateList barrel export.
 *
 * Admin UI component for managing kit template (packing list) definitions.
 * Provides real-time CRUD operations backed by Convex:
 *   - Create template (modal form)
 *   - Edit template name/description/active state (modal form)
 *   - Archive template (soft-delete with confirmation dialog)
 *   - Restore archived template
 *   - Duplicate template
 *
 * Usage:
 *   import { TemplateList } from "@/components/TemplateList";
 */
export { TemplateList } from "./TemplateList";
