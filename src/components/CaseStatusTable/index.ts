/**
 * CaseStatusTable — barrel export
 *
 * Re-exports the component and its prop types so consumers can import from
 * "@/components/CaseStatusTable" without referencing the internal module path.
 */

export { CaseStatusTable } from "./CaseStatusTable";
export type { CaseStatusTableProps } from "./CaseStatusTable";
