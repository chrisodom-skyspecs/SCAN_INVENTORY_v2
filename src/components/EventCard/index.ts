/**
 * EventCard — public barrel export.
 *
 * Re-exports the EventCard component and its associated TypeScript types
 * so consumers can import from the component directory:
 *
 *   import { EventCard } from "@/components/EventCard";
 *   import type { EventCardProps, EventVariant } from "@/components/EventCard";
 */

export { EventCard } from "./EventCard";
export type { EventCardProps, EventVariant, EventDotVariant } from "./EventCard";
export { default } from "./EventCard";
