/**
 * LabelPreviewModal — public exports.
 *
 * Re-exports the modal component and its prop types so consumers
 * can import from the component directory without referencing
 * internal file paths.
 */

export { LabelPreviewModal } from "./LabelPreviewModal";
export type {
  LabelPreviewModalProps,
  LabelPreviewCaseData,
} from "./LabelPreviewModal";
