/**
 * StopCard — standalone card for a single journey stop.
 *
 * Exports the StopCard component and its prop types.
 * This is a static/prop-driven display component with no Convex subscriptions.
 */

export { StopCard } from "./StopCard";
export type { StopCardProps, EvidenceThumbnail } from "./StopCard";
